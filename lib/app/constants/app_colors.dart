import 'package:flutter/material.dart';

const Color PRIMARY_COLOR = Colors.blueAccent;
const Color PRIMARY_DARK_COLOR = Colors.indigo;
const Color PRIMARY_LIGHT_COLOR = Colors.lightBlueAccent;

const Color TEXT_COLOR = Color.fromRGBO(16, 5, 55, 1);
const Color TEXT_HINT_COLOR = Color.fromRGBO(16, 5, 55, .6);

const Gradient PRIMARY_GRADIENT = LinearGradient(
    begin: Alignment.topLeft,
    end: Alignment.bottomRight,
    colors: [
      Color.fromRGBO(16, 5, 55, 1),
      Colors.indigo
    ]);
